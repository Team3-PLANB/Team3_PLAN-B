<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.planb_jeju.dao.RouteHistoryDao">

	<select id = "getMyHistory" resultType = "com.planb_jeju.dto.RouteHistory">
		SELECT r.routename, rd.route_date, rd.site
		FROM
			route r
		JOIN
			route_detail rd
		ON
			r.route_code = rd.route_code
		AND
			r.username = rd.username
		WHERE
			r.route_code = #{param1} AND r.username = #{param2}
	</select>
</mapper>