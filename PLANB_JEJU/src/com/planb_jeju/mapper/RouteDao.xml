<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.planb_jeju.dao.RouteDao">

	<!-- 전체 수 -->
	<select id="getCount" resultType="Integer">
		
		SELECT COUNT(*)
		FROM route
	</select>
	
	<!-- 현재 경로 번호 -->
	<select id="getRouteCode" resultType="Integer">
		
		SELECT MAX(route_code)
		FROM route
		WHERE username = #{username}
		group by username
	</select>
	
	<!-- 멤버 한명 셀렉 -->
	<select id="getRoute" resultType="com.planb_jeju.dto.Route">
		SELECT *
		FROM route
		WHERE route_code = #{param1}, username =#{param2}
	</select>
	
	<!-- 삭제 -->
	<delete id="delete">
	 	DELETE route WHERE route_code = #{param1}, username =#{param2}
	</delete>
	
	<!-- 수정 -->
	<update id="update" >
		UPDATE route 
		SET routename=#{routename}, 
		    partner_code=#{partner_code},
		    sday=#{sday},
		    eday=#{eday}
		WHERE route_code = #{route_code}, username =#{username}
	</update>
	
	<!-- 입력 -->
	<insert id="insert">
	
		<selectKey order="BEFORE" keyProperty="route_code" resultType="Int">
			Select MAX(route_code)+1 from route where username='a@naver.com' group by username
		</selectKey>
		
		INSERT INTO route(route_code, username, routename, partner_code, sday, eday)
	    VALUES( #{route_code}, #{username}, #{routename}, #{partner_code}, #{sday}, #{eday})
	</insert>
	
</mapper>
