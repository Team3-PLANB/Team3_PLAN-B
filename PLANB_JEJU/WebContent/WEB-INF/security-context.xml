<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" 
	xmlns:security="http://www.springframework.org/schema/security"
	xsi:schemaLocation="http://www.springframework.org/schema/beans 
						http://www.springframework.org/schema/beans/spring-beans.xsd
	  					http://www.springframework.org/schema/security 
	  					http://www.springframework.org/schema/security/spring-security.xsd" >


<!-- 시큐리티 적용X -->
<security:http pattern="/websockethandler/**" security="none" /> <!-- 알림관련 핸들러 -->
<security:http pattern="/css/**" security="none" />
<security:http pattern="/js/**" security="none" /><!-- 
<security:http pattern="/LoginJoin/**" security="none" /> -->

<security:http use-expressions="true">
	<security:csrf disabled="true"/>
 	
 	<!-- 로그인 -->
 	<security:form-login login-page="/LoginJoin/Join/NJoin.do"
 						 default-target-url="/MyPage/Schedule/schedule.do"
	    				 authentication-failure-url="/PLANA.do"/>  				 
	    					 
	<!-- 로그인/가입 페이지 접근 허용 -->
	<security:intercept-url pattern="/LoginJoin/**" access="permitAll" />    					 
	    					 
	 	<security:logout logout-success-url="/index.jsp" /> 
		<!-- <security:intercept-url pattern="/MyPage/**" access="hasRole('ROLE_USER')" /> -->
	</security:http>
<!-- password 인코딩 -->
<security:authentication-manager>
	<security:authentication-provider>
		<security:jdbc-user-service data-source-ref="driverManagerDataSource"
										users-by-username-query="select username as userid, password, 1 enabled from member where username = ?"
										authorities-by-username-query="select m.username, r.ROLE_CODE
																	   from member m join member_role r
																	   on m.username = r.username
																	   where m.username = ?" />

	</security:authentication-provider>
</security:authentication-manager>
<security:global-method-security pre-post-annotations="enabled" secured-annotations="enabled" />
</beans>